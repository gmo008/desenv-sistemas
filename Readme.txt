git clone https://github.com/gmo008/desenv-sistemas
git config --global user.name "geovana"
git config --global user.email "geovanamunizdeoliveira424@gmail.com"
git add.
git commit -m "escreva sua mensagem"
git push
